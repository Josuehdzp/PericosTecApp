package com.Pericos.ITSVC.App.Horarios;import android.content.Context;import android.database.sqlite.SQLiteDatabase;import android.database.sqlite.SQLiteOpenHelper;import android.util.Log;import java.io.File;import java.io.FileOutputStream;import java.io.InputStream;import java.io.OutputStream;public class DatabaseHelper extends SQLiteOpenHelper {    Context GContext;    String GDBPath;    String GDBName;    public DatabaseHelper(Context context, String name, int version) {        super(context, name, null, version);        this.GContext = context;        this.GDBName = name;        GDBPath = "/data/data/" + GContext.getPackageName()+"/databases/";    }    @Override    public void onCreate(SQLiteDatabase db) {    }    @Override    public void onUpgrade(SQLiteDatabase db, int oldVersion, int newVersion) {    }    public void CheckDb(){        SQLiteDatabase checkdb = null;        String Filepath = GDBPath+GDBName;        try {            checkdb = SQLiteDatabase.openDatabase(Filepath,null,SQLiteDatabase.OPEN_READONLY);        } catch (Exception e){}        if (checkdb == null){            CopyDatabase();        }        else {            SQLiteDatabase.openDatabase(Filepath, null, SQLiteDatabase.OPEN_READONLY);        }    }    public void CopyDatabase(){        this.getReadableDatabase();        try {            InputStream ios = GContext.getAssets().open(GDBName);            String filepath = GDBPath + GDBName;            OutputStream os = new FileOutputStream(filepath);            byte[] buffer = new byte[1024];            int lenght;            while (((lenght = ios.read(buffer)) > 0)){                os.write(buffer,0,lenght);            }            os.flush();            os.close();            ios.close();        } catch (Exception e) {}    }    //////////////////    @Override    public void onConfigure(SQLiteDatabase db) {        super.onConfigure(db);        Log.d("DBCONFIGURE","Database has been configured "); //TODO remove for Live App        db.disableWriteAheadLogging(); //<<<<<<<<<< un-comment to force journal mode    }}